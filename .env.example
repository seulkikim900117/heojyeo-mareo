# 이 파일은 "견본"입니다. 실제 값은 절대 여기에 적지 마세요!
# 사용법: 이 파일을 복사해서 .env.local 을 만들고, 거기에 실제 값을 넣으세요.
#   cp .env.example .env.local
# .env.local 은 .gitignore 에 등록되어 있어서 깃허브에 올라가지 않습니다.

# (지금은 프론트 목데이터/내장 계산만 사용하므로 필수 환경변수 없음)
# 나중에 DB 연결 시 예시:
# DATABASE_URL=
# NEXT_PUBLIC_SITE_URL=
