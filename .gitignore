# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 환경변수 파일 — 비밀번호/API키가 들어가므로 절대 깃허브에 올리지 않기!
# (.env.example은 예외 — 값 없이 "어떤 변수가 필요한지"만 알려주는 견본 파일)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel
